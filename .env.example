# ============================================
# The Mile Game - Environment Variables
# ============================================
# Copy this file to .env and adjust values as needed
# Usage: cp .env.example .env

# ============================================
# PORTS
# ============================================
# Frontend (Nginx)
FRONTEND_PORT=8081

# Backend (Go API) - Internal only, not exposed
BACKEND_PORT=8080

# PostgreSQL - Internal only
POSTGRES_PORT=5432

# ============================================
# DATABASE
# ============================================
DB_HOST=postgres
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=milegame

# ============================================
# BACKEND
# ============================================
# Gin mode: debug | release
GIN_MODE=release

# CORS Allowed Origins (comma-separated)
# Add all origins where the frontend will run
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8081,http://localhost

# ============================================
# FRONTEND
# ============================================
# API URL for frontend
# Use relative path /api when running through nginx
# Use http://localhost:8080/api for direct API access in dev
VITE_API_URL=/api

# WebSocket URL (optional - auto-detected if not set)
# Use ws://localhost:8080/ws for direct WebSocket access in dev
# VITE_WS_URL=ws://localhost:8080/ws
